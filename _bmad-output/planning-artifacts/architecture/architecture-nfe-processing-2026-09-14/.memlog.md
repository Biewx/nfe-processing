---
scope: Relatorio Mensal de Inteligencia de Compras (feature do PRD prd-nfe-processing-2026-09-14): elegibilidade, composicao do email via insights/analytics existentes, agendamento mensal, envio via Brevo
purpose: build-substrate + explicacao para o autor (projeto de estudo)
altitude: feature
updated: 2026-09-17T20:26
---

- (constraint) insights/analytics services (GetBestSupplierService, GetSavingsOpportunitiesService, GetProductPriceIncreaseService, SuppliersService) ja recebem companyId como parametro puro, nao dependem de request HTTP -- podem ser chamados diretamente por um job de background sem simular requisicao autenticada.
- (constraint) GetSavingsOpportunitiesService.getSavingsOpportunities(companyId) nao aceita filtro de mes/ano -- sempre usa 'compra mais recente por produto' (findLatestPurchasePerProduct). PRD FR-2 pede oportunidades 'do periodo' (mes anterior completo). Divergencia real a resolver.
- (constraint) Nao existe hoje nenhuma tabela/persistencia para registrar execucao do relatorio mensal por empresa (status, erro, timestamp). FR-6/SM-3 exigem falha de envio 'registrada e auditavel'.
- (constraint) package.json nao tem @nestjs/schedule nem nodemailer nem nenhuma lib de fila (BullMQ/etc) -- scheduling e envio de email sao greenfield dentro do projeto.
- (constraint) Convencao de DI atual: modulos NAO usam imports/exports do Nest entre si -- InsightsModule redeclara ExpensesService/AnalyticsRepository/PrismaService como providers proprios em vez de importar AnalyticsModule. Duplicacao de registro de provider entre modulos.
- (decision) [ADOPTED] Provedor de email: Brevo via SMTP (nodemailer), remetente individual verificado -- decidido no addendum do PRD por nao exigir dominio proprio. Revisitar quando houver dominio.
- (version) Stack herdada do projeto (CLAUDE.md): NestJS 11, Prisma 7 (Postgres), class-validator/class-transformer, xml2js, Jest. Nenhuma versao nova fixada ainda para scheduling/email.
- (decision) Paradigma: Pipeline orquestrado -- coordinator (RunMonthlyReportService) chama estagios em sequencia (elegibilidade -> composicao -> render -> envio -> registro), cada estagio um service de responsabilidade unica, seguindo o estilo ja usado no resto do projeto. Sem camada de ports/adapters formal.
- (decision) Scoping do periodo em savings_opportunities: estender GetSavingsOpportunitiesService para aceitar month/year opcionais (default = comportamento atual, compra mais recente) para nao quebrar /insights/savings_opportunities. O relatorio mensal sempre passa month/year explicitos do mes de referencia (mes anterior completo).
- (decision) Auditoria de envio: nova tabela Prisma (MonthlyReportRun) com um registro por (companyId, referenceMonth, referenceYear), status (PENDING/SENT/FAILED/SKIPPED), errorMessage opcional, scheduledAt, sentAt. Unique constraint (companyId, referenceMonth, referenceYear) garante idempotencia -- rodar o job 2x no mesmo ciclo nao reenvia.
- (decision) Distribuicao de envio (>250 emails/ciclo): job mensal (cron dia 1) calcula elegiveis e grava scheduledAt espalhado ao longo do dia nos registros MonthlyReportRun (status PENDING). Um segundo @Cron de intervalo curto (ex: a cada 10min) faz 'drain': busca PENDING com scheduledAt <= now, envia, atualiza status. Sem infra nova (sem Redis/BullMQ).
- (version) @nestjs/schedule 12.0.1 -- peerDependencies aceita @nestjs/common ^10||^11, compativel com o Nest 11 do projeto (verificado via npm/web em 2026-09-14). nodemailer 10.0.10 -- requer Node >=20, ships own typings (sem precisar de @types/nodemailer); projeto ja usa @types/node ^24, Node runtime presumido >=20.
- (event) Reviewer Gate rodada com 3 subagents em paralelo (rubric walker, verificacao de versoes, adversarial). Achados criticos confirmados por 2 revisores independentes: AD-4 original estava errada -- findPurchaseHistoryByProduct ignora month/year apesar do FiltersDto aceitar. Outros achados: diagrama de sequencia contradizia AD-6 sobre onde compose roda; AD-6 nao cobria overflow alem de 1 dia; falta wiring do ScheduleModule.forRoot(); risco de double-send em ticks sobrepostos do drain; peerDependencies do @nestjs/schedule citada errada (^10||^11 -- correto e ^11||^12); calculo de janela de mes feito de 3 formas diferentes e nao-coordenadas. Reviews completas em reviews/review-rubric-walker.md, reviews/review-version-check.md, reviews/review-adversarial.md.
- (decision) Fixes aplicados apos Reviewer Gate: AD-4 corrigida (findPurchaseHistoryByProduct passa a aceitar corte superior de data via month/year, simetrico a AD-3); nova AD-8 (helper unico getReferenceMonthWindow, timezone local de servidor herdado, sem introduzir UTC agora); AD-5 reescrita (status PENDING->SENDING->SENT|FAILED com transicao atomica, 1 sendMail por empresa com todos os usuarios em 'to', nao por usuario); AD-6 reescrita (overflow >300/ciclo distribui por varios dias, nao so 1 dia; composicao/render movidos pro drain, cron mensal so agenda; nota explicita sobre ScheduleModule.forRoot() em AppModule); diagrama de dependencia de modulo (AD-1) simplificado pra mostrar so as arestas que esta feature cria; Stack corrigida (@nestjs/schedule ^12.0.2, peerDeps real ^11||^12; nota sobre engines.node ausente no package.json).
- (event) spine finalized. Walkthrough explicativo publicado como Artifact: https://claude.ai/artifact/7TGeUvAyaFCrYn35nVnfmU
- (version) Reconfirmado @nestjs/schedule direto em registry.npmjs.org (nao apenas busca web) em 2026-09-14: latest = 12.0.2, peerDependencies = { @nestjs/core: '^11.0.0 || ^12.0.0', @nestjs/common: '^11.0.0 || ^12.0.0' }. Nota do Stack table completada para citar @nestjs/core tambem, nao so @nestjs/common.
- (event) Correcao pos-Reviewer-Gate encontrada durante implementacao da Story 1.2: AD-8 (Binds, Prevents, Rule) citava a query de elegibilidade como consumidora do helper getReferenceMonthWindow, contradizendo a excecao ja documentada na propria AD-2 (elegibilidade e lifetime, sem janela). Story 1.1 ja tinha sido implementada corretamente (sem usar nenhuma janela). Corrigido AD-8 para remover FR-1/elegibilidade do seu escopo, em ARCHITECTURE-SPINE.md e epics.md. Nenhum codigo precisou mudar -- so o texto da spine estava errado.
- (decision) AD-8 corrigida novamente: getReferenceMonthWindow move de monthly-report/utils para insights/utils/get-reference-month-window.ts, pois insights.repository (findLatestPurchasePerProduct, findPurchaseHistoryByProduct) precisa dela internamente, e insights nao pode depender de monthly-report sem inverter AD-1. monthly-report importa a funcao por caminho relativo (import de arquivo puro, nao e provider Nest -- mesmo padrao ja usado por FiltersDto entre analytics e insights).
- (event) Correcao encontrada na implementacao da Story 2.1 (nao em nenhuma revisao anterior): @nestjs/schedule ^12.0.2 e ESM puro (type:module), quebra o Jest/ts-jest CommonJS deste projeto com SyntaxError ao importar (Unexpected token export). A verificacao de versao feita na Architecture (e reconfirmada depois via registry.npmjs.org) checou peerDependencies mas nunca o formato de modulo nem rodou a suite de testes de verdade contra o pacote -- so a implementacao revelou isso. Trocado para @nestjs/schedule@6.1.3 (ultima versao antes do pacote pular de 6.1.3 direto pra 12.0.0 para acompanhar Nest 12): CJS, peerDependencies ^10||^11 batem exatamente com o Nest 11 do projeto. package.json e ARCHITECTURE-SPINE.md corrigidos.
- (event) code-review (bmad, effort high) rodado na branch feature/monthly-report-intelligence. Achados aplicados: (1) CONFIRMED -- GetProductPriceIncreaseService podia gerar alerta de fornecedor sem compra no mes de referencia (historico antigo virava 'current'); corrigido com filtro de janela na service, alem do corte superior ja existente no repository (AD-4 atualizada); (2) HTML nao escapado em render-monthly-report-email.service.ts (companyName/fornecedor/produto vem do XML da NFe) -- adicionado escapeHtml; (3) loops sequenciais paralelizados com Promise.all em run-monthly-report.service.ts (elegibilidade) e compose-monthly-report.service.ts (alertas por produto); (4) empresa elegivel sem usuario cadastrado agora e pulada no agendamento, evitando FAILED permanente previsivel; (5) limite conhecido documentado (nao corrigido): uma unica empresa com userCount > MAX_RECIPIENTS_PER_DAY estoura o teto sozinha, inerente a AD-5 (1 sendMail por empresa). Testes: 84/84 passando apos os fixes.
